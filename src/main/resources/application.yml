spring:
  application:
    name: Nomadly

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3307/nomadly_db
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8761

eureka:
  instance:
    hostname: localhost

